@model IndexViewModel
@{
    ViewData["Title"] = "Manage your account";
}

<h2>@ViewData["Title"].</h2>
<p class="text-success">@ViewData["StatusMessage"]</p>

<div>
    <h4>Profile Settings your account settings</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Password:</dt>
        <dd>
            @if (Model.HasPassword)
            {
                <a asp-controller="Manage" asp-action="ChangePassword">Change</a>
            }
            else
            {
                <a asp-controller="Manage" asp-action="SetPassword" class="btn-bracketed">Create</a>
            }
        </dd>
        <dt></dt>
        


        <dt>Name: </dt>
        <dd>
            @Model.FirstName @Model.LastName
        </dd>

        <dt>Phone Number: </dt>
        <dd>
            @Model.PhoneNumber
            @*@(Model.PhoneNumber ?? "None")
                @if (Model.PhoneNumber != null)
                {
                    <br />
                    <p>@Model.PhoneNumber</p>
                    <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Change</a>
                    <form asp-controller="Manage" asp-action="RemovePhoneNumber" method="post">
                        [<button type="submit" class="btn-link">Remove</button>]
                    </form>
                }
                else
                {
                    <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Add</a>
                }*@
        </dd>
        <dt>Address: </dt>
        <dd>
            @Model.Address
        </dd>
        <dt>Email: </dt>
        <dd>
            @Model.Email
        </dd>
        @*<dt>Two-Factor Authentication:</dt>
        <dd>
            <p>
                There are no two-factor authentication providers configured. See <a href="https://go.microsoft.com/fwlink/?LinkID=532713">this article</a>
                for setting up this application to support two-factor authentication.
            </p>*@
            @*@if (Model.TwoFactor)
                {
                    <form asp-controller="Manage" asp-action="DisableTwoFactorAuthentication" method="post" class="form-horizontal">
                        Enabled <button type="submit" class="btn-link btn-bracketed">Disable</button>
                    </form>
                }
                else
                {
                    <form asp-controller="Manage" asp-action="EnableTwoFactorAuthentication" method="post" class="form-horizontal">
                        <button type="submit" class="btn-link btn-bracketed">Enable</button> Disabled
                    </form>
                }*@
        @*</dd>*@
    </dl>
    <div>
        <a asp-action="EditPayment" asp-route-id="">Saved Payment Methods</a> 
        <br />
        <a asp-action="CreatePayment" asp-route-id="">Add Method of Payment</a>
        <br />
        <a asp-action="OrderHXDetail" asp-route-id="">Order History</a>
    </div> 
    <br />
    <a asp-action="CreatePayment" asp-route-id="@Model." class="btn btn-default">Edit Profile</a>


</div>
